# S2 · 修 Button `_t_us` bug + README cargo install

## 任务

1. **bug 修复**：`BridgeEvent::Button` 事件里 `_t_us` 字段当前被忽略，事件时间戳全是 0。找到产生该事件的位置，把 simavr 当前模拟时间正确填入。
2. **README 重写**：把现有的 alias 安装方式换成 `cargo install --git https://github.com/<repo>/moxin-longyuan`。一行命令搞定。

## 允许动的文件

- `bridge/moxin-simavr-bridge.c`（如果时间戳在 C 侧获取）
- `src/sim/bridge.rs` 或类似 Rust 侧的 BridgeEvent 解码处
- `tests/`（加一个测试验证按钮事件时间戳非零且单调递增）
- `README.md`（只动 Installation 章节）

## 验收

```powershell
cargo test
cargo clippy --all-targets
# 跑一个按钮 example,看事件日志里 t_us 不是 0
moxin run examples/button-led 2>&1 | Select-String "Button.*t_us=\d+"
```

按钮事件 `t_us` 字段非零，且后一次按下的 t_us 大于前一次。

README 里 Installation 章节只剩 `cargo install ...` 一行命令 + 一句先决条件（Rust 1.75+）。

## 约束

- 不顺手改其它事件类型
- 不改 BridgeEvent 的字段名或顺序（会破坏序列化兼容）
- README 不动 Installation 以外的章节

## commit message

`fix(S2): Button 事件时间戳 + README 改用 cargo install`
