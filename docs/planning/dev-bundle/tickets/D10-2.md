# D10-2 · GitHub Release v0.5.0

预计工时：1 小时
模块：发布

---

## 任务

打 v0.5.0 tag，写 release notes，挂 demo 视频链接。Phase 1 完成标记。

## 验收

GitHub Release 页面：
- tag: `v0.5.0`
- title: `v0.5.0 — Phase 1: AI-friendly hardware simulator`
- 含完整 release notes（见下）
- 链接到 demo 视频
- 链接到 AI 接入指南

## Release notes 模板

```markdown
# v0.5.0 — Phase 1 Complete · AI-friendly hardware simulator

## 🎬 看视频
- 中文：[B 站链接]
- English: [YouTube link]

## ✨ Highlights

- **AI 工具链**：`moxin run --output json` + `moxin status` + `moxin assert`
- **14 数字 + 6 模拟引脚仿真**：完整 ATmega328P 支持
- **9 元件库**：LED、按钮、数码管、蜂鸣器、电位器、电阻、面包板、杜邦线、Arduino Uno
- **跨平台 build**：Rust 单源码，macOS / Linux / Windows
- **元件 schema 体系**：components/*.toml + pin-anchors/*.json，建模部 / 前端部对齐

## 📥 安装

\`\`\`bash
cargo install --git https://github.com/<...>/moxin-longyuan
\`\`\`

或下载预编译二进制（如果 Phase 2 加了 CI 后）：
[Release artifacts](#assets)

## 🆕 新功能

- D6: `moxin status` —— 查询正在跑的 sim 状态
- D7: `moxin assert` —— DSL 断言，PASS/FAIL 退出码
- D8: `--output json` —— JSON Lines 模式给 AI 工具消费
- D8: AI 接入指南 + .cursorrules / CLAUDE.md 模板
- D3-D5: 蜂鸣器 / 数码管 / 电位器 / 被动元件 schema

## 🐛 修复

- D1-2: BridgeEvent::Button 字段名拼错（_t_us → t_us）
- D2-1: TUI sim 运行时第一个字符同时写 serial 和 buffer（加模式切换）
- D2-2: TUI 输入条光标 CJK / emoji 错位（unicode-width）
- D2-3: RunningSim::stop 泄漏 detach 线程
- D1-4: STM32 wire D13 别名误导
- D1-5: examples/stm32-blink 文件结构与 cmd_new 不一致

## 🚧 Phase 2 路线图

下面这些**故意没做**，预留 Phase 2：

- `moxin doctor` 命令
- 跨平台 CI release 二进制
- 扩展元件：DHT11 / HC-SR04 / 舵机 / LCD1602 / OLED / RGB LED / 光敏 / 红外 / 直流电机
- 扩展板：Nano / STM32F103 / RPi Pico / ESP32
- 国产芯片：CH32V / GD32 / HC32
- 被动元件 runtime（resistor 分压 / breadboard 节点连通）
- MCP Server
- 串口 RX 注入

## 📊 数据

- 代码：~4500 行 Rust + ~250 行 C
- 测试：约 60 个单测 + 4 个集成测试
- 时长：10 天高强度交付
- 团队：开发部（1 人）

## 🙏 致谢

建模部、设计前端部、财经部协作。详见各部门任务派发书。

---

**完整变更**：v0.1.0...v0.5.0
```

## 操作步骤

```bash
git tag -a v0.5.0 -m "Phase 1 complete"
git push origin v0.5.0
# 在 GitHub 网页上 Edit Release，贴 release notes
```

## 约束

- ❌ 不要在 release notes 里写"未完成"或"待办"，那些移到 issue
- ❌ 不要混杂中英文太乱（推荐：标题中英，正文中文为主，引用链接保留英文 tag）
- ✅ 必须挂视频链接

## Commit message

```
release: v0.5.0 — Phase 1 complete

含 release notes、视频链接、Phase 2 路线图。

ref: docs/sprint-plan.md D10-2
```
