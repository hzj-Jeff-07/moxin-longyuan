# D8-2 · 写 AI 接入指南 docs/ai-integration.md

预计工时：3 小时
模块：docs

---

## 任务

写 `docs/ai-integration.md`，教 Cursor / Claude Code 用户接入 MoXin 工具链。
这份文档是 MoXin 对外的"AI 友好"叙事支柱。

## 验收

文档至少包含：
1. **30 秒 quickstart**：3 行命令跑通最小例子
2. **moxin run --output json 输出格式**：贴示例，逐字段解释
3. **moxin status 用法**：3 个具体场景
4. **moxin assert 用法**：3 个具体场景
5. **Cursor 工作流**：完整截图 / 文字说明 AI 如何调试嵌入式代码
6. **Claude Code 工作流**：同上
7. **常见错误**：5 条 FAQ

文档发出后，在群里 / V2EX / Twitter 引流。

## 文档骨架

```markdown
# 用 AI 工具 + MoXin 调试嵌入式代码

> 适用：Cursor、Claude Code、其他能跑 shell 命令的 AI 编程工具

## 30 秒上手

\`\`\`bash
cargo install moxin
moxin new demo --board=uno && cd demo
moxin run --output json
\`\`\`

## 三件套：run / status / assert

### `moxin run --output json`
[JSON Lines 输出格式 + 逐字段解释]

### `moxin status`
[3 个具体用例]

### `moxin assert`
[3 个具体用例 + DSL grammar 简表]

## Cursor 工作流

[一个完整的例子：用户给一段有 bug 的 .ino，
Cursor 调用 moxin run / moxin assert / moxin status 自己定位 bug 并修复]

## Claude Code 工作流
[类似]

## .cursorrules 模板
[贴一份]

## CLAUDE.md 模板（给 Claude Code）
[贴一份]

## 常见错误 FAQ
1. moxin: command not found
2. simavr bridge not found
3. arduino-cli not found
4. status 返回 "no running sim found"
5. assert 总是 timeout
```

## 约束

- ❌ 不写 Rust 代码
- ❌ 不要在 README 重复完整内容（README 链过来）
- ✅ 至少 1 张 Cursor / Claude Code 操作截图
- ✅ 中英双语版本（开发部为主，英文版可以晚一天，D10 出）

## Commit message

```
docs(D8-2): AI 接入指南

教 Cursor / Claude Code 用户用 moxin run / status / assert 三件套
调试嵌入式代码。含完整工作流截图 + .cursorrules / CLAUDE.md 模板 + FAQ。

ref: docs/sprint-plan.md D8-2
```
