# D7-2 · 实现 assert DSL parser + matcher

预计工时：3 小时
模块：assert（新模块）

---

## 任务

实现 D7-1 设计的 assert DSL 解析器和匹配器。
不接 CLI（D7-3 才接），只做纯函数库。

## 验收

```bash
cd moxin-longyuan
cargo test assert::tests   # 全绿，至少 15 个测试覆盖各种 grammar
```

## 实现

新文件 `src/assert.rs`：

```rust
//! Assert DSL parser + matcher
//!
//! Grammar (Phase 1):
//!   assert    = subject SP predicate SP "within" SP duration
//!   ...

use anyhow::{Result, bail};

#[derive(Debug, PartialEq, Eq)]
pub enum Subject {
    Pin(String),                    // "D13" / "PA13"
    Serial,
    Led(String),                    // component id
    Buzzer(String),
}

#[derive(Debug, PartialEq, Eq)]
pub enum Predicate {
    High,
    Low,
    Between(u16, u16),
    Contains(String),
    On,
    Off,
    Frequency(u32),
}

#[derive(Debug, PartialEq, Eq)]
pub struct Duration {
    pub millis: u64,
}

#[derive(Debug, PartialEq, Eq)]
pub struct AssertExpr {
    pub subject: Subject,
    pub predicate: Predicate,
    pub timeout: Duration,
}

pub fn parse(s: &str) -> Result<AssertExpr> {
    // 简单递归下降或正则切分均可
    // 不要引入 nom / chumsky
    // ...
}

// 匹配：给定 snapshot 流，判断是否在 timeout 内满足
pub trait SnapshotStream {
    fn next(&mut self) -> Option<RunStateSnapshot>;
}

pub fn evaluate(
    expr: &AssertExpr,
    stream: &mut dyn SnapshotStream,
) -> AssertResult { ... }

#[derive(Debug)]
pub enum AssertResult {
    Pass { elapsed_ms: u64 },
    Fail { reason: String, elapsed_ms: u64 },
}
```

## 测试覆盖

```rust
#[test] fn parse_pin_high_simple() { ... }
#[test] fn parse_pin_with_arduino_d() { ... }
#[test] fn parse_pin_with_arm_pa() { ... }
#[test] fn parse_serial_contains_quoted() { ... }
#[test] fn parse_pin_between_two_numbers() { ... }
#[test] fn parse_duration_seconds() { ... }
#[test] fn parse_duration_milliseconds() { ... }
#[test] fn parse_invalid_grammar_returns_err() { ... }
#[test] fn parse_led_on() { ... }
#[test] fn parse_buzzer_frequency() { ... }

#[test] fn evaluate_pass_immediately() { ... }
#[test] fn evaluate_timeout() { ... }
#[test] fn evaluate_pass_after_wait() { ... }
// ... 至少 15 个
```

## 约束

- ❌ 不要引入 nom / chumsky 等 parser combinator
- ❌ 不要碰 CLI (main.rs)
- ❌ 不要碰 status 命令
- ❌ 不要碰 RunState 写入
- ✅ 允许新增 src/assert.rs 文件

## Commit message

```
assert(D7-2): assert DSL parser + matcher 实现

按 D7-1 grammar 实现纯函数库：parse(&str) → AssertExpr + evaluate
+ snapshot 流匹配。CLI 接入留 D7-3。

15 个单测覆盖：grammar / 错误 / 匹配 / 超时。

ref: docs/sprint-plan.md D7-2
```
