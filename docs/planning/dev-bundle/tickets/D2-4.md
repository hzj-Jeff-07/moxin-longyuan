# D2-4 · 删 tui.rs 末尾的 _project_marker 死代码

预计工时：5 分钟
模块：tui

---

## 任务

`src/tui.rs` 末尾有：

```rust
#[allow(dead_code)]
fn _project_marker(_p: &Project) {}
```

注释说"reserved for future"是糖衣。删掉，连同顶部不再需要的 `use crate::project::Project;`
（如果文件内其他地方不用 Project）。

## 验收命令

```bash
cd moxin-longyuan
grep -rn "_project_marker" src/    # 应无输出
cargo build --release              # 编译过
cargo test                         # 全绿
cargo clippy --all-targets         # 无新增警告
```

## 修改

1. 删函数 `_project_marker` 整体（含 `#[allow(dead_code)]`）
2. 检查 src/tui.rs 是否还需要 `use crate::project::Project;`
   - 若文件内其他地方仍用 `Project`，保留 use
   - 若不再用，删 use

## 约束

- ❌ 不要碰其他 #[allow(dead_code)] 标注（其他文件的死代码不在本 ticket 范围）
- ❌ 不要改 src/project.rs 的 Project 定义
- ✅ 只动 src/tui.rs

## Commit message

```
tui(D2-4): 删 _project_marker 死代码

注释说"reserved for future"是糖衣，没实际用途。删除。

ref: docs/sprint-plan.md D2-4
```

## 完成后输出

1. `git diff src/tui.rs`
2. `cargo build && cargo clippy --all-targets` 输出
